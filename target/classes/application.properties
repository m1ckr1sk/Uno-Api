# = Logging

# Level for loggers on classes inside the root package "uk.mickrisk" (and its 
# sub-packages). 
# Available levels are: TRACE, DEBUG, INFO, WARN, ERROR, FATAL, OFF
logging.level.uk.mickrisk = DEBUG

# Fine-tuning a specific logger (for a single class)
# logging.level.uk.mickrisk.controllers.UnoController = TRACE

# Specify the level for spring boot and hibernate's loggers
logging.level.org.springframework.web = DEBUG
# logging.level.org.hibernate = ERROR

# Log file location (in addition to the console)
#logging.file = /var/uno/logs/spring-boot-logging.log